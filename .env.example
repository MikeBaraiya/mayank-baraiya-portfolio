# Email Configuration - Gmail SMTP
GMAIL_EMAIL=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password
PORT=5000
VITE_API_URL=http://localhost:5000

# Optional: Advanced SMTP Settings
# Default values use Gmail (smtp.gmail.com, port 587, TLS)
# Uncomment and modify if using a different email provider

# SMTP_SERVICE=gmail                    # Email service name
# SMTP_HOST=smtp.gmail.com              # SMTP server address
# SMTP_PORT=587                         # Port: 587 (TLS) or 465 (SSL)
# SMTP_SECURE=false                     # true for port 465 (SSL), false for port 587 (TLS)

# Alternative Email Providers Examples:
# Outlook/Microsoft 365:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false

# SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false

# Zoho Mail:
# SMTP_HOST=smtp.zoho.com
# SMTP_PORT=465
# SMTP_SECURE=true
